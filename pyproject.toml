[project]
name = "django-todo-master"
version = "0.1.0"
description = "A comprehensive TODO application built with Django and Python."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2.8",
]
